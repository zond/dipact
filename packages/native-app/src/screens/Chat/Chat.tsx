import React from "react";
import { Text, View } from "react-native";

interface ChatProps {
  gameId: string;
}

const Chat = ({}: ChatProps) => {
  return (
    <View>
      <Text>Coming soon!</Text>
    </View>
  );
};

export default Chat;
